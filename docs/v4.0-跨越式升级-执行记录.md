# ΜΝΗΜΗ v4.0「星空图书馆」跨越式升级 · 执行记录（2026-09-09）

> 方案来源：GLM-5.3 五阶段路线图（A 阅读 / B 性能 / C 创新 / D 安全 / E 健康），推荐顺序 A→B→D→C→E。
> 本轮完成：Deploy 011（提交 8460b55 + 81bd9ca），smoke 39/39。

## 已完成

| 项 | 内容 | 状态 |
| --- | --- | --- |
| A1 三栏阅读架构 | 左目录（标题树+滚动侦听）· 中正文（双栏对照）· 右来源检查器，均可收起 | ✅ 收编 GLM-5.3 工作区实现，随本轮验证（+406 行） |
| A2 阅读器设置 | 字号/行宽/宋黑切换（localStorage mneme-reader） | ✅ 同上 |
| A3 阅读位置持久化 | localStorage LRU80，跨会话恢复 | ✅ v3.x 已有（方案盘点偏差） |
| A5 Voices 加固 | 回收总数服务端核账（overview.surveyTotal）；密码入口收敛（「解锁此卷」） | ✅ |
| A6 段落链接复制 | 标题复制按钮 | ✅ 收编实现 |
| A0 令牌收口 | tokens.css 字阶 token 第一批 | ◐ 部分（全站替换未完） |
| B1 路由代码分割 | 9 空间 React.lazy；首包 gzip 136.8→**73.1KB**（<90 门槛） | ✅ |
| B4 Graph 裁剪绘制 | 视口裁剪 + 世界贴图（v7.3/v7.4 已做，帧率探针 p95 12.5ms） | ✅ 已有 |
| B5 冒烟脚本化 | `npm run smoke` = v71–v74 四套 39 项 | ✅ |
| D1 CSP | Report-Only 观察（style-src unsafe-inline 放行滤镜） | ✅ |
| D2 登录限速 | 5 次/分钟/IP 失败退避，实测第 6 次 429 | ✅ |
| D4 响应头 | nosniff / no-referrer / SAMEORIGIN | ✅ |
| E1 夜间自动化 | 跟随系统 + 手动覆盖记忆 | ✅ v3.x 已有（方案盘点偏差） |

## 未完成（后续批次）

- A0 全站 ad-hoc 字阶/动效值替换；A4 双文档对照深化（现有双栏对照为基础）
- B2 Lighthouse 实测基线（本地环境无 Lighthouse）；B3 AbortController 全面接管
- C1 PWA / C2 每日星座 / C3 拼音检索 / C4 伏应矩阵 / C5 键盘导航层 / C6 阅读足迹
- D3 依赖审计（本地 npm 镜像不支持 audit 端点，须在官方 registry 环境跑）
- E2 高对比模式 / E3 阅读时长感知 / E4 站内偏好开关

## 边界与事实

- 「103+」为库主口述核账值，现以服务端常量 `SURVEY_TOTAL=103` 为单一来源（server.mjs，核账后更新）。
- 方案盘点中「阅读位置仅 SPA 内存存」与「无夜间跟随系统」两条与代码不符（均已实现），未重复开发。
