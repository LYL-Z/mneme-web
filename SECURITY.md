# 安全说明

本仓库是网站**引擎**。口令、绝密姓名、知识库正文、SQLite 快照不在版本库里。

## 请先不要做的事

- 不要在公开 Issue、讨论区或 Pull Request 里粘贴访问口令、管理口令、绝密口令、`privacy.local.json` 或任何 `私人资料/` 正文。
- 不要把本机 `.env`、数据库或 vault 打进镜像后开源。
- 不要把「私密层返回 404」理解成邀请去探测他人的线上实例。未授权扫描他人站点不在许可范围内。

## 设计上的边界

- 访问口令只挡住闲逛，不是多用户账号体系。
- 私密路径未解锁时与缺失资源同为 404，避免枚举。
- 绝密档案走 HttpOnly 会话 cookie；解锁范围按服务端规则，不按前端开关。
- 云模式（`MNEME_MODE=cloud`）默认 `MNEME_STRICT=1`：口令仍是内置开发值则拒绝启动。

## 如何披露

请使用 GitHub 仓库的 **Security advisory（私密）**。  
若 advisory 不可用，开 Issue 时只写现象与影响面，**不要附带口令、路径或正文摘录**。
